# Spiritech Backend Domain
# Used by vite.config.ts to inject __API_DOMAIN__ into the frontend.
# All API calls use this domain as the base URL.
spiribe=spiritest.duckdns.org

# Alternatively, use the VITE_ prefix for direct import.meta.env access:
# VITE_SPIRIBE=spiritest.duckdns.org
